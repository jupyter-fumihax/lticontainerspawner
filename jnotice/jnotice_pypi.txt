
# update
vi labextension/index.ts
vi package.json             # check version
vi pyproject.toml           # check version


# compile
npm install
npm run build
python3 -m build

# push to PyPI 
twine upload dist/*


# clean up
cd labextension_jnotice
npm run clean
rm -rf node_modules build lib static .webpack-cache package-lock.json

cd ..
cd jnotice
rm -rf build dist src/*.egg-info
